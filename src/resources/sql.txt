CREATE TABLE cliente(
id SERIAL PRIMARY KEY NOT NULL,
nome VARCHAR(100) NOT NULL,
rg BIGINT NOT NULL,
telefone VARCHAR(20) NOT NULL,
matricula BIGINT NOT NULL,
livrosAlugados INTEGER NOT NULL,
qntdelivrosalugados INTEGER NOT NULL
);

CREATE TABLE livro(
cod SERIAL PRIMARY KEY NOT NULL,
isbn BIGINT NOT NULL,
titulo VARCHAR(100) NOT NULL,
editora VARCHAR(100) NOT NULL,
autor VARCHAR(100) NOT NULL,
anoPublicacao VARCHAR(4) NOT NULL,
disponibilidade text NOT NULL,
qntdeTotalAlugado INTEGER NOT NULL
);

CREATE TABLE consulta (
codigo serial primary key,
horario timestamp,
prontuario varchar(50),
idPaciente int REFERENCES paciente(id),
finalizado boolean
);

CREATE TABLE itemreceituario (
id serial primary key,
posologia varchar(30),
codMedicamento int REFERENCES medicamento(codigo),
codConsulta int REFERENCES consulta(codigo)
);

DELETE FROM paciente;
ALTER SEQUENCE paciente_id_seq RESTART WITH 1;

SELECT * FROM paciente;